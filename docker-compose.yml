services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - TZ=Asia/Tokyo  # これを追加
    depends_on:
      - db
  db:
    image: postgres:15
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=pass
      - POSTGRES_DB=itemsdb
      - TZ=Asia/Tokyo  # これを追加